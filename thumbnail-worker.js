self.onmessage=async({data})=>{const{id,file,width}=data;try{const bitmap=await createImageBitmap(file,{resizeWidth:width,resizeQuality:'high',imageOrientation:'from-image'});const canvas=new OffscreenCanvas(bitmap.width,bitmap.height);canvas.getContext('2d').drawImage(bitmap,0,0);const dimensions={width:bitmap.width,height:bitmap.height};bitmap.close();const blob=await canvas.convertToBlob({type:'image/webp',quality:.82});self.postMessage({id,blob,...dimensions});}catch(error){self.postMessage({id,error:error.message});}};
